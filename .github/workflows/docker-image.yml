name: Docker Image CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Build the Docker image
      run: docker build . --file Dockerfile --tag sirenuix:$(date +%s)

    - name: Login to Docker Hub
      run: |
        echo sirenuix | docker login -u sirenuix --password-stdin

    - name: Push Docker Image to Docker Hub
      run: docker push sirenuix:$(date +%s)  
